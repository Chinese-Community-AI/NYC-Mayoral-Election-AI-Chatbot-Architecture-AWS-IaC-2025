// Minimal placeholder config for local development; will be updated later
const config = {
  appSync: {
    graphqlEndpoint: "",
    apiKey: "",
    region: "us-east-1",
  },
  apiUrl: "",
  jwt: {
    expiresIn: 86400,
    tokenPrefix: "Bearer ",
  },
};

export default config;
